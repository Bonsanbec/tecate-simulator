# GIS Data

Store normalized GIS data that has been imported from raw sources but is not yet runtime-ready.

Expected contents include normalized GeoJSON feature sets, source lineage reports, and corridor-specific extracts.

