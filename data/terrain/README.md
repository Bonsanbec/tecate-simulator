# Terrain Data

Store terrain source notes, DEM processing metadata, and intermediate terrain conversion outputs.

Raw DEM rasters should not be consumed by the runtime. Runtime terrain must be prepared under `generated/terrain` and packaged under `generated/packages`.

