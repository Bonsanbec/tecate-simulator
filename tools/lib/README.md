# Tool Libraries

This directory contains shared TypeScript helpers for CLI parsing, file access, coordinate conversion, tile creation, and validation.

Helpers must stay small and dependency-light.

