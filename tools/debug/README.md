# Debug Tools

Debug tools inspect project state and generated outputs.

They may print summaries or produce small debug files, but they should not mutate source data unless clearly named as converters.

