# Generators

Generators create deterministic derived data from normalized inputs.

Allowed generation includes tile manifests, building massing, secondary geometry descriptors, navigation inputs, and debug outputs.

