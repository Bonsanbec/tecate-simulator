# Packaging Tools

Packaging tools assemble generated outputs into runtime-ready package manifests.

Package manifests must include tile IDs, file references, checksums, coordinate origin, and source lineage where available.

