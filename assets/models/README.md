# Models

Store authored model source files or exported runtime model files here after they exist.

Landmark models must preserve stable IDs and source notes. Generic models must remain clearly separate from curated landmarks.

