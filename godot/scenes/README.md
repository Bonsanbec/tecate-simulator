# Scenes

Godot scenes define runtime composition.

`Main.tscn` starts the runtime scaffold. `WorldRoot.tscn` owns world systems. `PlayerRig.tscn` is a future navigation rig and is not instanced by default.
