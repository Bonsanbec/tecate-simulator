# Godot Runtime

This directory contains the Godot 4.x C# runtime project.

The runtime loads prepared offline manifests and assets. It must not ingest raw GIS, scrape data, process DEM rasters, or make authoritative procedural world decisions.

