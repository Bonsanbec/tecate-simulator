# Schemas

This directory stores JSON contracts shared by TypeScript tooling and Godot runtime packages.

Schemas are intentionally small. They document required shape and naming without replacing TypeScript or C# validation code.

