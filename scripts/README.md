# Scripts

This directory is reserved for small developer entry points that orchestrate existing tools.

Core data processing logic belongs in `tools/`, not here. Scripts should call documented tools and keep behavior transparent.

